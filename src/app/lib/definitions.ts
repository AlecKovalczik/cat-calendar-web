export type User = {
    id: string,
    username: string,
    email: string,
    password: string,
}

export type Task = {
    id: string,
    title: string,
    description: string,
    status: string,
}